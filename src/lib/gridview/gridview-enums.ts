export enum FilterMode {
	None,
	BeginsWith,
	Contains,
	Equals,
	NotEqual,
	DistinctList,
	DynamicList,
	DateRange
}
export enum PagingType {
	Auto,
	Manual,
	Disabled
}
export enum SelectMode {
	None,
	Single,
	Multi
}
export enum FieldType {
	String,
	Boolean,
	Numeric,
	Date,
	Html,
	Time
}